// function def for calculating area of circle 
function calculate_area(radius) {
	area = Math.PI * radius * radius;
	return area;
}

//to print on the console
var area = calculate_area(5);
console.log(area);

